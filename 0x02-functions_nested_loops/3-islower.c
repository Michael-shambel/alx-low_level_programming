#include "main.h"

/**
 * _islower - checking the lowercase character
 *@c: the character fot check
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
