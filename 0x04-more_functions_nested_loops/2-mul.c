#include "main.h"

/**
 * mul - will compute the multiplication of two numbers
 * @a: first iniger number
 *@b: second iniger number
 * Return: Always 0.
 */
int mul(int a, int b)
{

	return (a * b);
}
