#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char CAP, SMALL;

	for (CAP = 'a'; CAP <= 'Z'; CAP++)
	{
	putchar(SMALL);
	}
	for (CAP = 'A'; CAP <= 'Z'; CAP++)
	{
	putchar(CAP);
	}
	putchar('\n');
	return (0);
}
